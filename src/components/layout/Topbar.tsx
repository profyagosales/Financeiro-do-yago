/**
 * Cabeçalho simplificado:
 *  - h-14 topo (56px) + subnav opcional h-10.
 *  - Logo + FY a 12px (px-3) da esquerda.
 *  - Rotas centrais alinhadas (flex grow + justify-center).
 *  - Controles (tema, avatar, config) a 12px da direita.
 *  - Vidro / blur e uso de --clr-active-from para cor base.
 */
export default function Topbar() {
  return <div />;
}
